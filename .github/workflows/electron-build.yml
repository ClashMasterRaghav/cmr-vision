name: Build Electron App

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - uses: actions/checkout@v3
    
    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18
        cache: 'npm'
        cache-dependency-path: 'cmr-vision/package-lock.json'
    
    - name: Install dependencies
      run: cd cmr-vision && npm ci
    
    - name: Build React App
      run: cd cmr-vision && npm run build
    
    # You'll need to add electron-builder configuration to package.json
    # and add a script like "electron:build": "electron-builder"
    
    # - name: Build Electron App
    #   run: cd cmr-vision && npm run electron:build
    
    # - name: Upload Artifacts
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: ${{ matrix.os }}-build
    #     path: |
    #       cmr-vision/dist/*.exe
    #       cmr-vision/dist/*.dmg
    #       cmr-vision/dist/*.AppImage
    #       cmr-vision/dist/*.snap
    #       cmr-vision/dist/*.deb
    #       cmr-vision/dist/*.rpm 